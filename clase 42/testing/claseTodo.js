class Todo {
    constructor() {
        this.todo = [];
    }

    list() {
        return this.todo;
    }

    add(title) {
        let todo = {
            title: title,
            completado: false,
        };
        this.todo.push(todo);
    }

    complete() {
        if (this.array.forEach(element => {

            })
        }
    }