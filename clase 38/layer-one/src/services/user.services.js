const array = require('../models/data');


// Here we have a lot of services / functions that we can use in our application.
const userAll = () => {
    return array;
}

module.exports = {
    userAll,
};